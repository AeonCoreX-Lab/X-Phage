pulse core {
    atom access_level = "0"
    shadow error_msg = "Access Denied: Ghost Identity Missing"
    
    beam "Requesting Secure Access..."
    
    scan access_level {
        beam "Welcome, User."
    }
    
    // logic will bypass if access_level is 0
    beam error_msg
}
