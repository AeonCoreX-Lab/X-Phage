// run-pass: stdlib link + constants
~link "math"

pulse main() -> int {
    atom pi  = PI
    atom e   = E
    atom phi = PHI
    beam "PI  = "
    beam pi
    beam "E   = "
    beam e
    beam "PHI = "
    beam phi
    return 0
}
