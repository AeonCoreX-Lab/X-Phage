// X-Phage Console & Logging v1.0

pulse log_info(msg) {
    beam "[INFO] " + msg
}

pulse log_warn(msg) {
    beam "[WARN] " + msg
}

pulse log_error(msg) {
    beam "[ERROR] SYSTEM FAULT: " + msg
}

pulse console_clear() {
    bypass sys_clear {
        cmd: "clear"
    }
}
